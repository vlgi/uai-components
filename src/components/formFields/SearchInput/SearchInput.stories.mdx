import { Meta } from "@storybook/addon-docs";

<Meta title="Components/FormFields/SearchInput/Custom" />

# Custom variables for search input

you can use the same variables as input component.

obs: in the input attributes you can use everything that is described in the
input but the placeholder and tabindex are separated
