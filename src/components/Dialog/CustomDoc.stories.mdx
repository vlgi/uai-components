import { ArgsTable, Meta, Story } from '@storybook/addon-docs';

import Dialog from "./Dialog.svelte";

<Meta title="Components/Dialog/CustomDocs" />


# Dialog

This docs is different because Dialog have fixed position,
and apears above all elements.

This dialog is made using our `Modal` component.

<ArgsTable of={Dialog} />

## Usage Example
