import { Meta, ArgsTable } from "@storybook/blocks";

import SideMenu from "./SideMenu.svelte";

<Meta title="Components/SideMenu/CustomDocs" />

# SideMenu

This docs is different because side menu have fixed position, and apears above all elements.

<ArgsTable of={SideMenu} />
